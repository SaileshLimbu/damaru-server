export enum Roles {
  EmulatorAdmin = 'EmulatorAdmin',
  SuperAdmin = 'SuperAdmin',
  AndroidUser = 'AndroidUser'
}

export enum SubRoles {
  AndroidAdmin = 'AndroidAdmin',
  AndroidAccount = 'AndroidAccount'
}
